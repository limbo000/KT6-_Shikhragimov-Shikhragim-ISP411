<Page x:Class="Pet.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pet.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1000"
      Title="ИС Товары для животных - Авторизация"
      Background="White">

    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <Label Content="Логин"></Label>
            <TextBox x:Name="LoginTextBox"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"></TextBox>
            <Label Content="Пароль"></Label>
            <PasswordBox x:Name="PasswordBox"
                         HorizontalContentAlignment="Center"
                         VerticalContentAlignment="Center"></PasswordBox>
            <Button x:Name="LoginButton" Content="Войти"
                    Margin="10" Click="LoginButton_Click"></Button>
        </StackPanel>
    </Grid>
</Page>
